#!/usr/bin/perl -w
use strict;

use lib 'lib';
use lib 'tools';

use MT;
use Backup;

my $mt = MT->new
    or die MT->errstr;

MT::Backup->backup( OutputCallback => sub { print @_ } ,  Compress => 1 )
    or die MT::Backup->errstr;
