# $Id$

my $loaded;
BEGIN { print "1..1\n" }
use MT::ErrorHandler;
use MT;
use MT::Object;
use MT::App;
use MT::App::CMS;
use MT::App::Comments;
use MT::App::NotifyList;
use MT::App::Search;
use MT::App::Trackback;
use MT::App::Upgrader;
use MT::App::Viewer;
use MT::App::ActivityFeeds;
use MT::Atom;
use MT::AtomServer;
use MT::Author;
use MT::Blog;
use MT::Builder;
use MT::Callback;
use MT::Category;
use MT::Comment;
use MT::Config;
use MT::ConfigMgr;
use MT::DateTime;
use MT::Entry;
use MT::FileInfo;
use MT::FileMgr;
#use MT::FileMgr::DAV;
#use MT::FileMgr::FTP;
use MT::FileMgr::Local;
#use MT::FileMgr::SFTP;
use MT::Image;
use MT::ImportExport;
use MT::IPBanList;
use MT::L10N::de;
use MT::L10N::en_us;
use MT::L10N::es;
use MT::L10N::fr;
use MT::L10N::ja;
use MT::L10N::nl;
use MT::L10N;
use MT::Log;
use MT::Mail;
use MT::Notification;
use MT::ObjectDriver;
use MT::ObjectDriver::DBI::mysql;
use MT::ObjectDriver::DBI::postgres;
use MT::ObjectDriver::DBI::sqlite;
use MT::ObjectDriver::DBI;
use MT::ObjectDriver::DBM;
use MT::Permission;
use MT::Placement;
use MT::Plugin;
use MT::PluginData;
use MT::Promise;
use MT::Request;
use MT::Sanitize;
use MT::Serialize;
use MT::Session;
use MT::TBPing;
use MT::Template;
use MT::Template::Context;
use MT::TemplateMap;
use MT::Trackback;
use MT::Upgrade;
use MT::Util;
use MT::XMLRPC;
use MT::XMLRPCServer;
use MT::Tag;
use MT::ObjectTag;
use MT::TaskMgr;
use MT::Task;
$loaded++;
print "ok 1\n";
END { print "not ok 1\n" unless $loaded }
