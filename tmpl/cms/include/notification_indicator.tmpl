<mt:if name="count_notification_dashboard">
<div class="row">
  <div class="col-12">
    <div class="alert alert-warning">
      <mt:if name="scope_type" ne="user">
      <a href="<$mt:var name="mt_url"$>?__mode=dashboard">
      </mt:if>
        <__trans phrase="You have <strong>[quant,_1,message,messages]</strong> from the system." params="<mt:var name="count_notification_dashboard">">
      <mt:if name="scope_type" ne="user">
      </a>
      </mt:if>
    </div>
  </div>
</div>
</mt:if>

